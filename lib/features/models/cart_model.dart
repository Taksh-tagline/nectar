class CartModel {
  String image, title, subTitle, price, quantity;

  CartModel(
      {required this.title,
      required this.image,
      required this.price,
      required this.quantity,
      required this.subTitle});
}
